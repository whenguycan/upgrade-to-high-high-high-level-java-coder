<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demolog.dao.OrderDao"> <!-- 这儿关联那个dao层 -->
    <insert id="addOrder">
        insert into t_order (`content`) values (#{content})
    </insert>
</mapper>